# fixture

## Endpoints

README.md Endpoints table fixture (drift): the route/panel fixtures declare a
`demo` UI host but this table lists no host, so lab-ui-check must report the
UI as missing from README.md's Endpoints table.

| UI | URL |
|----|-----|

## Something else

Not part of the Endpoints section — a stray host here would NOT be checked.
