apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: capstone
  namespace: capstone
spec:
  strategy:
    canary:
      steps:
      - setWeight: 10
      - analysis:
          templates:
          - templateName: latency-p95
