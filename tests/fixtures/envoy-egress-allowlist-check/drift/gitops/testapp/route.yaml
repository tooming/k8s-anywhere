apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: testapp
  namespace: testapp
spec:
  parentRefs:
    - name: eg
      namespace: lab-gateway
  hostnames:
    - testapp.127.0.0.1.nip.io
  rules:
    - backendRefs:
        - name: testapp
          port: 80
