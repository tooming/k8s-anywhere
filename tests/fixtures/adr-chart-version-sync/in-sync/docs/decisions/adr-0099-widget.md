# ADR-0099 — Widget (fixture)

## Decision

### Chart + version

- **Chart:** `widget-org/widget` `1.2.3` (`appVersion: 0.9.0`; pin lives in
  `gitops/platform/widget.yaml`'s `targetRevision` — this note read "v1.2.2"
  until the fixture author bumped it).
- **Source:** `https://example.test/charts`
