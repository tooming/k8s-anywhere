# No-configmap-data fixture: the ConfigMap exists but carries no mimir.yaml key at all
# (e.g. renamed by mistake) — the script must not silently treat this as "no paths to check".
apiVersion: v1
kind: ConfigMap
metadata:
  name: mimir-config
  namespace: observability
data:
  other-file.yaml: |
    unrelated: true
