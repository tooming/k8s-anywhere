# Fixture Makefile: only an `up` target exists — the README references `make bogus`,
# which does not, so readme-check must flag the drift.
REQUIRED_TOOLS := docker kubectl

up: ## bring the lab up
	@echo up
