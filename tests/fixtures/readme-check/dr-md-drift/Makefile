# Fixture Makefile for the docs/DR.md tool-list drift test. Mirrors the real
# one's shape: a REQUIRED_TOOLS line and `<target>: ... ## <desc>` help entries.
REQUIRED_TOOLS := docker kubectl helm

up: ## bring the lab up
	@echo up
