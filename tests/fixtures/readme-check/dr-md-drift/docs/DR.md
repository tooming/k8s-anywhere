# DR fixture with drift

`helm` is in REQUIRED_TOOLS but missing from this preflight line:

```sh
make preflight   # check tools (brew install: docker kubectl)
make up          # bring the lab up
```
