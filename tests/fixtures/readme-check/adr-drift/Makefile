# Fixture Makefile for readme-check tests. Mirrors the real one's shape:
# a REQUIRED_TOOLS line and `<target>: ... ## <desc>` help entries.
REQUIRED_TOOLS := docker kubectl

up: ## bring the lab up
	@echo up
