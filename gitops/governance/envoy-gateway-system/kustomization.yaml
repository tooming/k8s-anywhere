# Governance overlay for the envoy-gateway-system namespace (RFC #294 standard tier).
# Applies the standard-tier LimitRange container defaults. Delivered by the
# envoy-gateway-system-governance Application generated by gitops/platform/governance-appset.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: envoy-gateway-system

resources:
  - ../base/limitrange-standard.yaml
