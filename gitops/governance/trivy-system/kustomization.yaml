# Governance overlay for the trivy-system namespace (RFC #294 standard tier).
# Applies the standard-tier LimitRange container defaults. Delivered by the
# trivy-system-governance Application generated by gitops/platform/governance-appset.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trivy-system

resources:
  - limitrange.yaml
