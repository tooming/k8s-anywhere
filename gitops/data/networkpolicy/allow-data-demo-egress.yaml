# Allow egress from data-demo pods to RabbitMQ and Valkey (ADR-0016).
# rabbitmq-load talks to the management HTTP API on 15672.
# valkey-load talks to the Valkey server on 6379.
# Both generators stay within the data namespace — no cross-namespace egress needed.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-data-demo-egress
  namespace: data
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - rabbitmq-load
          - valkey-load
  policyTypes:
    - Egress
  egress:
    - ports:
        - port: 15672
          protocol: TCP
        - port: 6379
          protocol: TCP
      to:
        - podSelector: {}
