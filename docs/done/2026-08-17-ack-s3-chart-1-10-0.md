# Bump ACK (AWS Controllers for Kubernetes) S3 chart `1.9.0` → `1.10.0`

(CHARTER **Core Values** §"Everything as code" + general hardening;
executor-fallback upstream-currency gap analysis 2026-08-17, this run's first
pass, reached via `executor.prompt.md` STEP 6b — the entire "Now / next" lane is
gated (the three standing GitLab→Forgejo migration items plus the `verifyImages`
Enforce flip, the O4 CI-rejection-gate, and the legacy capstone `Deployment`
removal, all still gated on unconfirmed maintainer-confirmation issues #631/#633,
re-checked this run — both issues carry a fresh comment from a live-cluster
session ~20 minutes before this run started, but neither confirms the actual ask;
#631 is still chasing Harbor host-capacity instability, #633 found a *new*
prerequisite gap, the `argo-rollouts` Application itself failing to sync since
2026-08-11), and Planner STEP 2's own intake pass found zero ungroomed issues
(only #631/#633 open, both correctly-labeled standing trackers, not work
requests) and zero un-RFC'd 🟡 items anywhere in ROADMAP.md (verified: the file
has no `- [ ] 🟡` line) — this is a genuine upstream-currency gap-analysis
finding, not manufactured filler. **No prerequisites — executor may pick up
immediately.**

ACK isn't covered by `docs/dependency-register.md`'s systematic sweep (it has no
dedicated ADR — Terraform-bootstrapped/GitOps-deployed tools without a single
binding ADR are out of that register's scope by construction), so it wasn't swept
by this run's prior currency passes (Valkey, GitLab, `docker:29`). Verified
directly (not assumed, ADR-0004): GitHub's tags listing for
`aws-controllers-k8s/s3-controller` shows `v1.10.0` above the previously-pinned
`v1.9.0`, with no pre-release between them (one **minor** release, not a major
bump — in scope for direct executor/upgrade-drafter action). Fetched the tagged
`helm/Chart.yaml` directly: `version`/`appVersion` both read `1.10.0`. The
release's only functional change: `feat: add ABAC field to Bucket` (#241, adding
the `abac` field this lab's own fetch of the tagged CRD schema confirms is
present — matching AWS's real S3 ABAC feature launched 2025-11-20) plus a routine
ACK runtime/code-generator bump (`v0.62.0` → `v0.62.1`, #244) — no change to any
key this lab's `ack-s3.yaml` Application sets in `valuesObject` (`aws.*`,
`installScope`, `podSecurityContext.*`, `securityContext.*`, `resources.*`).

Bumped `gitops/platform/ack-s3.yaml`'s `targetRevision: 1.9.0` → `1.10.0`, with a
header-comment note documenting the finding above. Updated `tests/ack-s3.bats`'s
chart-pin assertions (retitled the "pins chart version" test to `1.10.0` +
the ABAC-field citation, added a "does not pin the stale 1.9.0 version" guard,
kept the existing 1.8.2 guard). Updated `docs/decisions/context.md`'s ACK
citation (`chart 1.9.0` → `chart 1.10.0`) so
`scripts/context-doc-version-sync-check.sh` (`make ci`'s drift gate) stays green
— this is exactly the recurrence class that check exists to catch (a 2026-08-12
session found this same citation two real bumps stale before the guard existed).
No `docs/dependency-register.md` row to update (out of scope per above).

**ADR-0004 caveat:** this remote clusterless session cannot verify the ACK S3
controller still reconciles a live `Bucket` CR against moto post-bump on a real
cluster. Rollback path: revert `targetRevision`; ArgoCD re-syncs the prior chart
version on next reconciliation; no CRD/CR schema *removal* in this bump — `abac`
is a new optional field — so no data-loss risk.

## PR

https://github.com/tooming/k8s-anywhere/pull/1203
