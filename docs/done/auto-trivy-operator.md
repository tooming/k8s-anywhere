# Trivy Operator continuous scanning + SBOMs — PR 1 (operator + namespace + NetworkPolicy + Alloy scrape)

- [x] **Trivy Operator continuous scanning + SBOMs — PR 1 (operator + namespace + NetworkPolicy + Alloy scrape)** (CHARTER **Objective O1** + CHARTER goal *supply-chain security end-to-end*, RFC #156 / ADR-0022) — Added `gitops/platform/trivy-operator.yaml` (auto-synced ArgoCD Application, chart `aqua/trivy-operator` v0.30.0 from `https://aquasecurity.github.io/helm-charts/`, namespace `trivy-system`, wave 1; all six scanners enabled per ADR-0022: `vulnerabilityScannerEnabled`, `configAuditScannerEnabled`, `rbacAssessmentScannerEnabled`, `exposedSecretScannerEnabled`, `sbomGeneration`, `clusterComplianceEnabled`; `excludeNamespaces: kube-system,kube-public,kube-node-lease`; ADR-0022 §"Footprint controls" memory limits: operator 256Mi, trivy 512Mi, nodeCollector 64Mi; 5Gi vuln-DB cache PVC). Added `gitops/trivy-system/namespace.yaml` (PSA `baseline` labels — scan-job pods unpack arbitrary OCI artifacts per ADR-0022 §"Per-namespace profile"). Added `gitops/platform/trivy-extras.yaml` (wave 0 Git-path Application pre-creating the namespace with PSA labels before the Helm release — mirrors kyverno-extras pattern). Default-deny NetworkPolicy overlay at `gitops/trivy-system/networkpolicy/kustomization.yaml` (shared baseline + `allow-trivy-metrics-from-observability.yaml` TCP 8080 from Alloy pods in `observability` + `allow-trivy-egress-vdb.yaml` TCP 443 to `0.0.0.0/0` for vuln-DB refresh from ghcr.io). New auto-synced `Application` `gitops/platform/trivy-system-networkpolicy.yaml` (wave 4, `LoadRestrictionsNone`). New `prometheus.scrape "trivy_operator"` job in `gitops/platform/observability-alloy.yaml` targeting `trivy-operator.trivy-system.svc.cluster.local:8080` (60s interval). Also added missing kyverno:8000 egress to `gitops/observability/networkpolicy/allow-alloy-egress-external.yaml` (gap from kyverno-engine PR). `docs/dependency-tree.md` updated with TRIVY subgraph, wave-4 table row, integration edge, notes entry. New `tests/trivy-operator.bats` (40 structural assertions). **Deferred to next planner item**: `grafana/dashboards/lab-trivy.json` (PR split per executor note — dashboard JSON would exceed ~400 line budget). (auto/trivy-operator)
