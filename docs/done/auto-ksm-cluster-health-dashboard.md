# Lab — Kube State Metrics cluster-health dashboard

- [x] **Lab — Kube State Metrics cluster-health dashboard** (CHARTER **Objective O5**, due **2026-09-30**; auto/ksm-cluster-health-dashboard) — Added `grafana/dashboards/lab-ksm.json` ("Lab — Cluster Health (KSM)"): 11-panel dashboard providing a K8s resource state overview using KSM metrics already scraped by the existing `prometheus.scrape "ksm"` block in `gitops/platform/observability-alloy.yaml` (no new scrape job needed). Stat-row panels: KSM pod running (`kube_deployment_status_replicas_available{namespace="observability",deployment=~"ksm.*"}`); ArgoCD sync state (`argocd_app_info{name="ksm"}`); node readiness count (`kube_node_status_condition{condition="Ready",status="true"}`); KSM version (`kube_state_metrics_build_info` — label lookup). Pod phase distribution row: Running / Pending / Failed / Succeeded across all namespaces (`sum(kube_pod_status_phase{phase=...})`). Timeseries: deployment replica health (available vs desired sum); PVC phase by namespace+claim (`kube_persistentvolumeclaim_status_phase`); KSM watch health rate by resource (`rate(kube_state_metrics_watch_total[5m])`). All panels use real Mimir data with `X-Scope-OrgID: lab`; panels show "No data" naturally until KSM emits the relevant series (ADR-0004 compliant). No HTTPRoute (KSM has no web UI). Extended `tests/observability.bats` with 10 new structural assertions (file exists, uid, Mimir datasource, `kube_pod_status_phase`, `kube_state_metrics_build_info`, `kube_deployment_status_replicas_available`, `kube_persistentvolumeclaim_status_phase`, `kube_node_status_condition`, no fabricated data, dependency-tree mention). Updated `docs/dependency-tree.md` with a KSM dashboard row in the integration table.
