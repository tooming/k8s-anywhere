# Kyverno engine + observability — PR 1 (engine + NetworkPolicy)

- [x] **Kyverno engine + observability — PR 1 (engine + NetworkPolicy)** (CHARTER **Objective O1**, RFC #153 / ADR-0019) — Added `gitops/platform/kyverno.yaml` (auto-synced ArgoCD Application, chart `kyverno/kyverno` v3.3.4 from `https://kyverno.github.io/kyverno/`, namespace `kyverno`, wave 1; single-replica per controller with ADR-0019 §"Footprint controls" memory limits: admission 256Mi, background 128Mi, cleanup 64Mi, reports 128Mi). Added `gitops/kyverno/namespace.yaml` (PSA `baseline` labels — webhook TLS `fsGroup` carve-out per ADR-0019 §"Per-namespace profile"). Added `gitops/platform/kyverno-extras.yaml` (wave 0 Git-path Application sourcing `gitops/kyverno/`, applies the namespace before the Helm release schedules pods — same pattern as vault-extras; also serves as landing zone for the cosign-bootstrap ConfigMap). Default-deny NetworkPolicy overlay at `gitops/kyverno/networkpolicy/kustomization.yaml` (shared baseline + `allow-kyverno-webhook-from-apiserver.yaml` TCP 9443 from kube-apiserver ipBlock `10.43.0.1/32` + `allow-kyverno-metrics-from-observability.yaml` TCP 8000 from Alloy pods in `observability`). New auto-synced `Application` `gitops/platform/kyverno-networkpolicy.yaml` (wave 4, `LoadRestrictionsNone`). `docs/dependency-tree.md` updated with KYVERNO subgraph. **Deferred to follow-up issue**: Alloy scrape job + `grafana/dashboards/lab-kyverno.json` + `tests/kyverno.bats` + dependency-tree Alloy scrape edge (PR split per executor note — oversized combined PR). (auto/kyverno-engine)

## PR

https://github.com/tooming/k8s-anywhere/pull/170
