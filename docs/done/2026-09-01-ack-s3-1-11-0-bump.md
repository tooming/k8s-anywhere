# Bump ACK S3 controller chart `1.10.0` → `1.11.0`

(CHARTER **Core Values** §"Everything as code" + general hardening; upgrade-drafter fallback, executor.prompt.md STEP 6b — this run's fourteenth cycle: the "Now / next" lane remained fully gated and PLANNER found no ungroomed intake or un-RFC'd 🟡 item. Routine currency sweep re-checked ACK S3, last reviewed 2026-08-17.)

Verified directly (not assumed, ADR-0004) via the real Atom feed (`github.com/aws-controllers-k8s/s3-controller/releases.atom`, ISO-8601 `<updated>` `2026-08-28T20:15:55Z`): `v1.11.0` is one release past the pinned `v1.10.0`.

**Schema compatibility verified via a real clone.** `git diff v1.10.0 v1.11.0 -- helm/Chart.yaml helm/values.yaml` shows the version/appVersion/`image.tag` bump plus exactly one new key: `featureGates.IgnoreFieldDrift: false` (a new opt-in feature, default off). This Application's `valuesObject` sets no `featureGates` override at all, so this is a complete no-op here — matching the exact "verify the whole diff, not just the version line" precedent the prior `1.9.0`→`1.10.0` bump (2026-08-17) established.

Upstream's own release names the real feature: "Support tags at CreateBucket time from Spec.Tagging" plus an ACK runtime bump to `v0.63.0`.

**This Application is ALWAYS-ON** (automated sync) — this pin takes effect on the next ArgoCD reconciliation.

Bumped `gitops/platform/ack-s3.yaml`'s `targetRevision: 1.10.0` → `1.11.0` and its header comment. Updated `tests/ack-s3.bats`'s pin assertions (retitled, added a negative assertion for the superseded `1.10.0`). Updated `docs/decisions/context.md`'s version citation (the only other place this chart's version is tracked — no `docs/dependency-register.md` row exists for this component).

**ADR-0004 caveat.** This remote clusterless session cannot verify the ACK S3 controller pod actually restarts cleanly on the new chart version on a live cluster, or that Bucket reconciliation against moto continues working. Rollback path: revert `targetRevision` back to `1.10.0`.

## PR

(filled in after PR creation)
