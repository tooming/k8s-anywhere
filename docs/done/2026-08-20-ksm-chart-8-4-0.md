# Bump kube-state-metrics chart `8.3.1` → `8.4.0`

(CHARTER **Core Values** §"Everything as code" + general hardening; executor-fallback
upstream-currency sweep 2026-08-20 — reached via `executor.prompt.md` STEP 6b: the
"Now / next" lane was re-checked and remains fully gated (the two sequentially-blocked
GitLab→Forgejo migration items plus the legacy capstone `Deployment` removal on issue
#633, unconfirmed; issue #1229's O4 CI-rejection-gate `KUBECONFIG` ask, also
unconfirmed — both re-checked, no new comments since the prior cycle). **No
prerequisites — executor may pick up immediately.**

This cycle also swept several other components for currency before landing on this
one, each confirmed already current (no action groundable):
- **Kyverno** — chart `3.8.2` (appVersion `v1.18.2`) already the newest *stable* chart
  tag; `kyverno-chart-3.9.0-rc.x` tags exist but are pre-release only, and `3.8.2`
  already carries the GHSA-79gf-7frw-68m9 backport this repo's own prior investigation
  already confirmed.
- **Trivy Operator** — `trivy-operator-0.35.0` confirmed the newest tag in the
  `aquasecurity/helm-charts` repo directly.
- **cert-manager** — `v1.21.1` confirmed the newest stable release tag.
- **External Secrets Operator** — `helm-chart-2.9.0` confirmed the newest chart tag.
- **KEDA** — `v2.20.2` confirmed the newest tag in `kedacore/charts`.
- **Velero** — `velero-12.1.0` confirmed the newest tag in `vmware-tanzu/helm-charts`.
- **Cilium** — `v1.18.13` confirmed the newest `1.18.x` tag.
- **Argo Rollouts / Envoy Gateway** — investigated but out of scope for a bump this
  cycle: Envoy Gateway `v1.9.0` remains correctly deferred per ADR-0008's own
  2026-08-18 Re-evaluation-log entry (breaking Gateway API CRD bump, needs live-cluster
  `helm template` verification this remote session cannot do) — re-confirmed, not
  re-bumped, no new finding.

Alongside the kube-state-metrics bump below, this cycle also found and fixed two
`docs/dependency-register.md` rows (**Alloy**, **node-exporter**) whose "Last
reviewed" cells cited only "ADR-0034 authored" (2026-08-07) — never a real currency
check, the same imprecise-citation pattern this session's earlier cycles already fixed
for the register's Loki and Tempo rows. Both are now backed by a real, dated
verification: Alloy's chart `1.11.1` (tag `helm-chart/1.11.1`, 2026-08-06) confirmed
the newest chart tag directly (the `v1.19.0-rc.x` tags visible in the same tag list are
app-level pre-releases, not chart releases); node-exporter's chart `4.56.1` confirmed
against the `prometheus-node-exporter` chart's own `main`-branch `Chart.yaml`, and
`prometheus/node_exporter` confirmed to have zero published GitHub security
advisories. Neither needed a version bump — both rows now cite the real check
performed, not just the ADR's authoring date.

Verified directly (not assumed, ADR-0004): the tagged `Chart.yaml` at
`kube-state-metrics-8.4.0` shows `version: "8.4.0"`, `appVersion: "2.20.0"` — up from
`8.3.1`'s `appVersion: "2.19.1"`, a real application-version bump this time (the prior
`8.3.0`→`8.3.1` bump was packaging-only). The upstream `kubernetes/kube-state-metrics`
`v2.20.0` release notes name one explicit security fix — a Go toolchain bump to
`1.26.6` remediating `GO-2026-5038` — plus a `client-go` bump to `v0.36.3` and a set of
additive-only metrics (pod-disruption-reason tracking, admission-policy object
metrics, HPA scale-behavior-tolerance metrics, `kube_node_spec_pod_cidrs`, DRA
`kube_pod_resourceclaim_info`, init-container state/exit-code tracking, ephemeral-
volume label generation, PV access-mode metrics, allowlist wildcard support) and a
watch-sharding performance change. The only named breaking change — "Custom Resource
State (CRS) metrics are now feature-frozen" — does not apply: this repo's
`valuesObject` in `gitops/platform/observability-ksm.yaml` sets no
`customResourceState:` key. A byte-level diff of the chart's `values.yaml` between the
two tags confirms every key this repo's `valuesObject` actually sets
(`fullnameOverride`, `selfMonitor.enabled`, `securityContext.{enabled,runAsNonRoot,
runAsUser,runAsGroup,seccompProfile.type}`, `containerSecurityContext.
{allowPrivilegeEscalation,privileged,readOnlyRootFilesystem,capabilities.drop}`,
`resources.{requests,limits}`) keeps the identical shape and chart defaults — no
schema change affects this Application's rendered manifest.

Bumped `gitops/platform/observability-ksm.yaml`'s `targetRevision: 8.3.1` → `8.4.0`.
Updated `docs/decisions/adr-0034-lgtmp-observability-stack.md`'s table row + added a
new Re-evaluation log entry (existing entries kept, appended before). Updated
`tests/securitycontext-observability.bats`'s chart-pin assertions (retitled the "pins
chart version" test to `8.4.0`, added a "does not pin the stale 8.3.1 version" guard,
kept the existing 8.0.0/8.1.3/8.2.0/8.3.0 guards). Updated
`docs/dependency-register.md`'s kube-state-metrics row "Last reviewed" cell, plus the
Alloy and node-exporter rows' citations described above.

**ADR-0004 caveat:** this remote clusterless session cannot verify kube-state-metrics
still reconciles and reports the new metrics correctly post-bump on a real cluster.
Rollback path: revert `targetRevision`; ArgoCD re-syncs the prior chart version on the
next reconciliation; no CRD change in this bump, so no data-loss risk.

## PR

https://github.com/tooming/k8s-anywhere/pull/1290
