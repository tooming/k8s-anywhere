# Bump `redis_exporter` sidecar `v1.87.0-alpine` → `v1.88.0-alpine`

CHARTER **Core Values** §"Everything as code" + general dependency hygiene.
Upgrade-drafter fallback sweep (`executor.prompt.md` STEP 6b), second cycle of
this run — the "Now / next" lane is still fully gated on the standing
maintainer-confirmation issues #631/#632/#633 (re-checked, still unconfirmed),
and the first cycle's candidate for this sweep (`gitops/platform/kargo.yaml`)
already landed as PR #718.

- Component: `oliver006/redis_exporter` sidecar container in
  `gitops/data/valkey/statefulset.yaml` (Valkey is always-on / auto-synced,
  ADR-0018). A plain container image tag, not a Helm chart — enumerated per
  upgrade-drafter's STEP 2 "plain Deployment/StatefulSet image:" walk.
- From → To: `v1.87.0-alpine` → `v1.88.0-alpine`. Patch bump, not major.
- Why this version: confirmed `v1.88.0` is the newest tag directly against
  the source repo's own git tags
  (`git ls-remote --tags github.com/oliver006/redis_exporter`), then
  confirmed the exact `v1.88.0-alpine` variant exists on Docker Hub
  (`hub.docker.com/v2/repositories/oliver006/redis_exporter/tags`) — the
  `-alpine` suffix isn't guaranteed to track every plain release 1:1, so
  checked the specific variant this repo pins, not just the bare tag.
- Compatibility: the release adds one new metric
  (`stream_entries_added_total`) with no flag/env-var changes (per the
  GitHub release notes — treated as a lower-confidence source per ADR-0004
  since it's a web-fetched summary, but corroborated by this being a small,
  focused exporter with a long history of additive-only patch releases). No
  CVE is involved — routine version currency. This container's config is
  minimal (`REDIS_ADDR`/`REDIS_PASSWORD` env vars only, no custom
  command/flags), so the upgrade surface exposed to breakage is small.
- Updated `tests/data-layer.bats`: the pin assertion now checks for
  `v1.88.0-alpine`.
- No ADR references this exporter's exact version (ADR-0010/ADR-0018 name
  the tool, not a pinned version), so no ADR edit needed.
- `make ci` green (locally: lint/drift/dependency-tree/ADR-sync checks all
  pass; kubeconform/kustomize/terraform/bats/helm-chart-pin-check aren't
  installed in this remote sandbox and run for real in GitHub Actions).

## PR

(filled in after PR creation)
