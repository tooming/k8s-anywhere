# Add ADR-0030 to docs/decisions/README.md's index; fix dependency-register.md's now-stale scope note

Janitor cleanup (`executor.prompt.md` STEP 6b, JANITOR fallback role — this
run's tenth cycle, Now/next starved by #631/#633, currency-bump and prior
janitor lenses both exhausted for this pass). Found via a stale-doc-reference
sweep of `docs/dependency-register.md`'s own "Scope note" section, which
stated (as a factual claim): "ADR-0030 exists but isn't in that index, so
it's out of scope here too, matching the index's own boundary." Verified
directly (ADR-0004): `grep -n "ADR-0030" docs/decisions/README.md` returns
nothing — confirming the gap the register's own scope note was already
flagging, but had not been fixed.

## What changed

- `docs/decisions/README.md`: added the missing `ADR-0030` index line,
  matching the existing one-line-per-ADR format.
- `docs/dependency-register.md`: updated the Scope note's ADR-count math to
  stay internally consistent now that ADR-0030 is indexed (29→30 ADRs
  indexed; the "seven policy/posture ADRs" bucket becomes eight, adding
  ADR-0030 alongside ADR-0016 as the same "policy enforced via an
  already-listed tool" shape — k3s's own row already exists via ADR-0027).
  The "20 ADRs name 22 distinct tools" figure is unchanged (ADR-0030 doesn't
  name a new tool). Did **not** add ADR-0030 as a second citation to the
  existing k3s row — mirrors the precedent already set by Cilium's row
  (cites only ADR-0014, the CNI-choice ADR, not also ADR-0016's
  default-deny-pattern ADR that applies to it).

## Why this matters

`docs/decisions/README.md` is this repo's canonical ADR table of contents —
a session (or a maintainer) skimming it for "what decisions exist" would
have missed ADR-0030 (k3s version-pinning governance) entirely, despite this
same run's own `arch/k3s-1-36-3-rfc` and `auto/k3s-1-36-3` PRs actively
using and extending that ADR. `dependency-register.md`'s own scope note had
already correctly identified the gap in prose but nothing had acted on it —
left it stale for a week (ADR-0030 was authored 2026-07-19, the register was
written on/after 2026-07-29 already referencing the gap).

## `make ci`

Green. No test asserts an exact ADR count in either file, so nothing else
needed updating.

## PR

(filled in after PR creation)
