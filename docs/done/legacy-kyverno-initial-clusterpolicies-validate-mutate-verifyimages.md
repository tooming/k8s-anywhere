# Kyverno initial ClusterPolicies (validate + mutate + verifyImages)

- [x] **Kyverno initial ClusterPolicies (validate + mutate + verifyImages)** (CHARTER **Objective O1** + gates **Objective O4**, RFC #153 / ADR-0019) — Added four `ClusterPolicy` manifests under `gitops/kyverno/policies/`: `require-pod-security-restricted.yaml` (validate Enforce; skips namespaces with PSA `baseline`/`privileged` label via `namespaceSelector` — auto-adapts to ADR-0017 carve-outs; checks `seccompProfile.type=RuntimeDefault` at pod level + `allowPrivilegeEscalation=false`, `runAsNonRoot=true`, `capabilities.drop=["ALL"]` per container); `disallow-latest-tag.yaml` (validate Enforce; `foreach` + `deny` conditions rejecting `:latest` tag or no-tag images); `add-default-seccomp.yaml` (mutate; `patchStrategicMerge` with `+(seccompProfile)` conditional anchor — injects `seccompProfile.type=RuntimeDefault` only when absent); `verify-image-signatures.yaml` (verifyImages Audit + `failurePolicy: Ignore` scoped to `artifactory.127.0.0.1.nip.io/**`; references `cosign-public-key` Secret in `kyverno` namespace seeded by `scripts/cosign-bootstrap.sh` — flip to Enforce once the cosign CI signing pipeline lands). New auto-synced ArgoCD `Application` `gitops/platform/kyverno-policies.yaml` (sync-wave 5, after engine wave 1; plain directory mode — no kustomize). Extended `tests/kyverno.bats` with 17 new structural tests covering Application shape, all four policy files, PSS backstop skip labels, seccomp patchStrategicMerge shape, Artifactory registry scope, Audit mode + failurePolicy Ignore for verifyImages. **Known operational note:** `disallow-latest-tag` (Enforce) will block re-creation of the capstone pod once it uses `:latest` — a follow-up planner item should pin the capstone image tag once the cosign pipeline lands. (auto/kyverno-policies PR #TBD)
