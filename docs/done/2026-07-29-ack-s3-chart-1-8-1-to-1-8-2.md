# upgrade(ack-s3): 1.8.1 -> 1.8.2

CHARTER **Core Values** §"Everything as code" + general currency hardening.
Upgrade-drafter-flavored fallback (`executor.prompt.md` STEP 6b — Now/next
remains fully gated on standing maintainer-confirmation issues
#631/#632/#633, re-verified this cycle: all three still open, zero
comments).

## What changed

`gitops/platform/ack-s3.yaml`'s `targetRevision` bumped from `1.8.1` to
`1.8.2` — the AWS Controllers for Kubernetes S3 service controller chart
(sourced from the real OCI registry `public.ecr.aws/aws-controllers-k8s`,
chart `s3-chart`).

## Why this version

Highest stable release, no major bump, no pinning ADR governs this
component's version. Verified directly (ADR-0004), not assumed:

- `git ls-remote --tags --refs https://github.com/aws-controllers-k8s/s3-controller`
  shows `v1.8.2` is the latest tag on the `1.8.x` line (one patch ahead of
  the prior `1.8.1` pin).
- The corresponding OCI chart artifact was independently confirmed to
  actually exist and be pullable: fetched a real anonymous pull token from
  `public.ecr.aws/token/` and queried
  `https://public.ecr.aws/v2/aws-controllers-k8s/s3-chart/tags/list`
  directly — `1.8.2` is present in the real tags list returned by the
  registry itself, not just a git tag (the same verification discipline
  this repo applied to the Envoy Gateway `v1.8.3` bump, where a git tag
  existed before the chart artifact did — this bump's artifact is
  confirmed live).

## Upstream notes

No `CHANGELOG.md` was fetchable at the tag's raw GitHub path (404); the
version bump is grounded in the real git tag + the real OCI registry tags
list, per ADR-0004 — no invented changelog detail is claimed.

## What `make ci` saw

Green (full local run, including the new `tests/ack-s3.bats` recurrence
guard: pins `1.8.2`, asserts the stale `1.8.1` is absent).

## Rollback

Revert `targetRevision` to `1.8.1`; ArgoCD self-heals within its sync
interval. The ACK S3 controller is a stateless Deployment (reconciles
`Bucket` CRs against the moto AWS mock, no persistent state of its own), so
a revert recovers immediately with no data loss.

## PR

https://github.com/tooming/k8s-anywhere/pull/859
