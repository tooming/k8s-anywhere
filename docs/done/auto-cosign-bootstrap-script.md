# cosign-bootstrap.sh day-0 seam

- [x] **cosign-bootstrap.sh day-0 seam** (CHARTER **Objective O4** enabler, RFC #153 / ADR-0019 §"Cosign keypair management") — Added `scripts/cosign-bootstrap.sh`: generates a cosign keypair under `infra/secrets/cosign/` (gitignored via new `infra/secrets/` entry in `.gitignore`); seeds the public key into ConfigMap `cosign-public-key` in namespace `kyverno` using the `--dry-run=client -o yaml | kubectl apply -f -` idempotency pattern (mirrors `vault-bootstrap.sh` / `garage-bootstrap.sh`). NOT wired into `make up` yet — that's a follow-up planner item. Added `tests/cosign-bootstrap.bats` (10 structural assertions: script exists + is executable, `cosign generate-key-pair` invocation, `infra/secrets/cosign` path, ConfigMap name + namespace, `--dry-run=client`, `kubectl apply -f -`, and two conditional checks against `verify-image-signatures.yaml` that skip if the kyverno-policies PR is not yet merged). (auto/cosign-bootstrap-script)
