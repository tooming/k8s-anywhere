# Planner grooming — RFC #205 + RFC #206 (2026-06-15)

**Run:** planner fallback (executor lane was empty; all Now/next items complete).
**Branch:** `plan/rfc-205-206-groomed`
**PR:** _to be filled in after open_

## What was groomed

### RFC #205 — PSS-restricted enforcement for the argocd namespace

Architect filed 2026-06-14. The two-phase rollout decision (no `infra/` touch for
Phase 1; `infra/modules/argocd/values.yaml` update for Phase 2) resolves the 🟡
blocker. Per WAYS-OF-WORKING.md §2 the architect's RFC IS the approval — groomed
into two 🟢 executor items without additional human sign-off:

- `auto/argocd-pss-warn-audit` — Phase 1: `gitops/argocd/namespace.yaml` with
  warn+audit labels only; new `gitops/platform/argocd-extras.yaml` Application.
- `auto/argocd-pss-enforce` — Phase 2: `infra/modules/argocd/values.yaml`
  securityContext hardening + `enforce: restricted` label flip. Blocked on
  Phase 1 being verified green in cluster by maintainer before pickup.

Issue #205 closed with a grooming comment referencing the ROADMAP items.

### RFC #206 — NetworkPolicy fan-out for envoy-gateway-system namespace

Architect filed 2026-06-14. The coarse-grained namespaceSelector decision for proxy
egress (avoids per-Service enumeration) resolves the 🟡 blocker. Groomed into one
🟢 executor item:

- `auto/envoy-gateway-system-networkpolicy` — four allow files + kustomization +
  `gitops/platform/envoy-gateway-system-networkpolicy.yaml` Application.

Issue #206 closed with a grooming comment referencing the ROADMAP item.

## Remaining 🟡 items awaiting architect RFCs

- **O4 cosign-CI + verifyImages Enforce flip** — awaiting RFC for the exact
  GitLab CI `cosign sign` step shape, `make up` wiring sequence, and Audit→Enforce
  flip timing. Executor must not pick this up unprompted.
- **O6 capstone-demo wall-clock target** — awaiting RFC clarifying wall-clock scope,
  Tempo verification approach, and Makefile boundary.
- **ADR-0017 audit — vault PSA baseline** — hold pending a Vault chart update that
  drops `cap_ipc_lock`. No RFC or executor item needed now.
