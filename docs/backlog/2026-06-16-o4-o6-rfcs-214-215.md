# Architect run 2026-06-16 — O4 + O6 RFCs (#214, #215)

**Run context.** Executor lane was empty (all Now/next items [x]); an open
plan PR #213 covers RFC #205+#206 grooming (pending merge). Falling through
the STEP 6b chain: PLANNER already ran (PR #213); ARCHITECT has two 🟡 items
without RFCs (O4, O6). This run files those RFCs and updates ROADMAP.md.

## RFCs filed this run

| RFC | 🟡 ROADMAP item | Decision summary |
|-----|-----------------|-----------------|
| #214 | O4 — cosign signing in GitLab CI + verifyImages Enforce flip | Three executor items: (1) `make cosign-bootstrap` wired into `make up` after `garage-bootstrap`; (2) `sign-image` job in `.gitlab-ci.yml` using `bitnami/cosign:2` with `--allow-insecure-registry` + `COSIGN_EXPERIMENTAL=0`; (3) verifyImages Audit→Enforce flip after CI signing confirmed. |
| #215 | O6 — make capstone-demo wall-clock target | Demo-only scope (not full `make up`). `scripts/capstone-demo.sh` implements five steps: argocd wait, ExternalSecret Ready, curl 200, Tempo trace via port-forward, 900 s budget. Makefile target `capstone-demo` + `tests/capstone-demo.bats` (clusterless) + `docs/DR.md` section. |

## What the planner should do next run

Groom RFC #214 into three 🟢 executor items (one per acceptance-criteria
group). Groom RFC #215 into one 🟢 executor item (single-PR per
acceptance criteria). Both RFCs are self-contained — no prerequisite ADR
or `infra/` change required.

## No new ADR authored this run

Neither O4 nor O6 introduces a new technology choice — they implement the
already-decided cosign flow (ADR-0019) and a Makefile/script item (no ADR
domain). The architect's decision is captured in the RFC issues.
