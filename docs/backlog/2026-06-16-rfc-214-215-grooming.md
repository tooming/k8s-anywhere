# Planner run 2026-06-16 — RFC #214 + #215 groomed into 🟢 executor items

**Run context.** Executor lane was blocked: ArgoCD PSS Phase 1 in-flight (PR #218),
ArgoCD PSS Phase 2 blocked on in-cluster verification, envoy-gateway-system NP in-flight
(PR #219). Falling through STEP 6b chain to PLANNER role. Two ungroomed `rfc`-labeled
issues found: RFC #214 (O4) and RFC #215 (O6).

## Issues groomed this run

### RFC #214 — O4 completion: cosign CI signing + verifyImages Enforce flip

Filed by architect run 2026-06-16. Groomed into three 🟢 executor items (in dependency order):

| Branch | Item | Dependency |
|--------|------|-----------|
| `auto/cosign-make-up-wiring` | Add `make cosign-bootstrap` phony target; wire into `make up` after `garage-bootstrap`, before `grafana-gitsync-bootstrap`; extend `tests/cosign-bootstrap.bats` | None (scripts/cosign-bootstrap.sh already merged) |
| `auto/cosign-ci-sign-step` | Add `sign` stage + `sign-image` job in `.gitlab-ci.yml` using `bitnami/cosign:2`, `COSIGN_EXPERIMENTAL=0`, `--allow-insecure-registry` | Wait for `auto/cosign-make-up-wiring` |
| `auto/cosign-enforce-flip` | Flip `verify-image-signatures.yaml` from Audit+Ignore → Enforce+Fail; extend bats | Wait for `auto/cosign-ci-sign-step` **AND** maintainer confirms `.sig` tag in Artifactory |

All three are RFC-approved 🟢 per WAYS-OF-WORKING.md §2 (Makefile + CI changes that are
normally 🟡 are explicitly specified in the architect's binding RFC Decision).

### RFC #215 — O6: make capstone-demo wall-clock target

Filed by architect run 2026-06-16. Groomed into one 🟢 executor item:

| Branch | Item | Dependency |
|--------|------|-----------|
| `auto/capstone-demo-target` | `scripts/capstone-demo.sh` (5-step: argocd wait → ExternalSecret → curl → Tempo trace → 900 s budget); `make capstone-demo` Makefile target; `tests/capstone-demo.bats` (clusterless); `docs/DR.md` §Capstone demo | None (standalone) |

Makefile + script change is RFC #215-approved 🟢 per WAYS-OF-WORKING.md §2.

## Issues closed this run

Both RFC issues (#214, #215) are labeled `rfc` — commenting, labeling `groomed`, and
closing them as part of this plan PR.

## No new 🟡 items surfaced

The cross-cutting section now has only the ADR-0017 vault audit reminder (no RFC needed)
and the two newly groomed entries. The "Now / next" lane is stocked with four buildable
🟢 items for the next executor runs.
