# Incident log — severity scheme & real incident history

A documented incident classification (severity) scheme, plus a running log of real
incidents this lab has actually hit — filed to close the one *structural* (not just
cadence) gap [`docs/dora-audit-readiness.md`](dora-audit-readiness.md) names for
Pillar 2 (Q6/Q8): no severity tiering existed anywhere in the repo, and no dedicated
artifact captured "what broke, in production-shape terms, and why" distinct from the
fix commit itself. Every entry below is a real, already-observed incident — cited
against the issue comment, PR, or session where it was actually found and (where
applicable) fixed — never a fabricated or illustrative example
([ADR-0004](decisions/adr-0004-no-fabricated-content.md)).

**Scope note.** This lab is a solo-operator, clusterless-by-default learning platform
(see [ROADMAP.md](../ROADMAP.md) rule #2) — not a continuously operated production
system. **No paging and no escalation path is an intentional non-goal here, not a
silent absence** (mirrors the gap `docs/dora-audit-readiness.md`'s Q7 already names
explicitly): there is no on-call rotation to page and no second responder to escalate
to. What this scheme *does* provide is a consistent way to classify blast radius when
an incident is found, and an honest, cumulative record of root causes — the two things
Q6 and Q8 flag as missing.

## Severity scheme

| Severity | Definition | Expected response (solo operator) |
|---|---|---|
| **P0** | Whole-lab-down, or a data-loss risk (a stateful namespace's backup/restore path is broken, a destructive `make` target misbehaves). | Fix in the same session it's found — nothing else takes priority. |
| **P1** | A single always-on component is down or degraded, or a security-relevant gap (an admission policy not enforcing, a NetworkPolicy hole, an exposed credential). | Fix in the same session, or the very next one if mid-investigation. |
| **P2** | An on-demand/heavy component (Harbor, TiDB, Istio, Longhorn, Kargo's pipeline) is broken, or a non-blocking functional defect in an always-on component. | Track as a ROADMAP/backlog item; fix on the next relevant executor pass. |
| **P3** | Cosmetic, documentation drift, or a low-value inefficiency with no functional impact. | Filler-lane item — cheap to defer indefinitely. |

## How to log a new incident

Copy this row shape (mirrors the `| Field | Content |` template already used in
[`docs/dora-audit-readiness.md`](dora-audit-readiness.md)'s "Template for a new
question"):

| Field | Content |
|---|---|
| Date | *when it was found (not necessarily when it started)* |
| Severity | *P0–P3, per the scheme above* |
| Component(s) | *namespace / ArgoCD Application / script affected* |
| Detection | *how it was found — `make status`, a live-cluster investigation, a CI failure, a maintainer report* |
| Root cause | *the actual mechanism, not just the symptom* |
| Fix | *PR number, or "fixed live" if it was a non-GitOps-managed change (Vault data, CI variables)* |
| Time to resolve | *wall-clock from detection to fix landing, or "unresolved" if still open* |
| Follow-up | *any recurrence guard added, or "none needed"* |

## Real incident history

| Date | Severity | Component(s) | Detection | Root cause | Fix | Time to resolve | Follow-up |
|---|---|---|---|---|---|---|---|
| 2026-07-29 | P0 | Cilium (cluster-wide — apiserver connectivity) | Live-cluster investigation while working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631) | Cilium agents lost apiserver connectivity after a k3d node IP reshuffle. | Fixed live via `make cilium-up` (no PR — a runtime re-bootstrap, not a GitOps change). | Same session | None needed — a k3d node IP reshuffle is an environmental event, not a repo defect. |
| 2026-07-29 | P1 | `artifactory` namespace NetworkPolicy | Bringing Artifactory up to test issue [#631](https://github.com/tooming/k8s-anywhere/issues/631) | The `artifactory` namespace's default-deny NetworkPolicy had no intra-namespace allow, so `artifactory-oss` could never reach its own bundled `postgresql` — the `wait-for-db` init container hung forever and the pod never started. This means Artifactory had likely never actually come up successfully in this lab before this was found. | PR #884 (`allow-artifactory-intra-namespace.yaml`, mirroring the existing `harbor` carve-out in ADR-0016). | Same session | Verified live: `artifactory-oss` reached `6/6 Running` after the fix. |
| 2026-07-29 | P1 | Vault (`vault-unsealer`, namespace `vault`) | Found live while working the same cluster-wide network outage as the Cilium row above (`gitops/vault/unsealer.yaml`'s own header comment records the finding). | `vault-0` restarted during the outage and came back sealed. The unsealer's own `vault status` call had no timeout and hung on the dead connection, wedging its `while true` loop forever — the container never crashed (the shell process was still "running"), so kubelet had no signal to restart it. Vault stayed sealed for **4+ days**, silently breaking every `ExternalSecret` refresh cluster-wide (`ClusterSecretStore "vault"` reported not-ready the whole time, but nothing surfaced that anywhere visible). | PR #884 (same PR as the `artifactory` NetworkPolicy row below) — added a `timeout` bounding each `vault` call so a dead connection can't wedge the loop, plus a heartbeat file + liveness probe so kubelet can detect and restart a wedged loop even if some other hang mode slips past the timeout. | Same session | Mechanical guard added: the heartbeat-file + liveness-probe pair gives kubelet a structural way to catch this failure *class* (a silently-wedged-but-not-crashed container), not just this specific hang. Cited as a documented real incident in `docs/dora-audit-readiness.md`'s Stateless component criticality tiers (Vault, P1) before it was backfilled into this table. |
| 2026-08-04 | P1 | `envoy-gateway-system` egress NetworkPolicy / Harbor | Live-cluster investigation while working issues [#631](https://github.com/tooming/k8s-anywhere/issues/631)/[#633](https://github.com/tooming/k8s-anywhere/issues/633) | `allow-envoy-proxy-backend-egress` allowlists specific backend namespaces by name, and `harbor` was never added when its route landed — every request to Harbor's HTTPRoute timed out (`downstream_remote_disconnect`) because the default-deny floor silently dropped the proxy's egress to it. | PR #968. | Same session | Verified live after merging: `curl http://harbor.127.0.0.1.nip.io:8080/api/v2.0/systeminfo` returned 200. |
| 2026-08-04 | P1 | Harbor admin credential (Vault + GitLab CI variables) | Live-cluster investigation while working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631) | `secret/harbor/registry` in Vault held a password from `scripts/vault-bootstrap.sh`'s random-generation fallback that was never actually Harbor's real admin password, so the CI job's `docker login` could never have succeeded. | Fixed live — Vault's `secret/harbor/registry` and the GitLab `HARBOR_USER`/`HARBOR_PASSWORD` CI variables were updated to match Harbor's real current admin credential (`harbor-admin-creds` secret). Not GitOps-managed, no PR. | Same session | None yet — this data isn't declaratively managed, so there's no mechanical guard against it drifting again; a future ROADMAP item could add a CI check that exercises `docker login` against Harbor on a schedule. |
| 2026-08-04 | P2 | GitLab CI (no runner ever registered) | Triggering a pipeline via the API while working issues [#631](https://github.com/tooming/k8s-anywhere/issues/631)/[#633](https://github.com/tooming/k8s-anywhere/issues/633) — it sat in `pending` forever, `GET /runners` returned `[]` | No GitLab Runner has ever been registered against this lab's GitLab instance, so `.gitlab-ci.yml` has never actually executed a pipeline in this lab's history — not just a symptom of the two fixes above, but the reason neither issue #631's signed-image confirmation nor #633's Kargo-promotion confirmation could ever have succeeded, regardless of the registry/network state. | PR #1026 (`gitlab/docker-compose.yml`, docker executor, privileged `docker:29-dind` service). | Same session (landed 2026-08-05, per #631/#633's 2026-08-05 comments) | Verified live: `GET /runners` shows `lab-docker-runner`, `status: online`, and a triggered pipeline's `build-and-push` job actually executes (previously nothing ran). This closed the "nothing can run at all" root cause — a *different* blocker (Harbor/node health, next row) is what #631/#633 hit immediately after. |
| 2026-08-05 | P1 | k3d node disk pressure (`k3d-k8s-lab-server-0`) | Live-cluster investigation while working issues [#631](https://github.com/tooming/k8s-anywhere/issues/631)/[#633](https://github.com/tooming/k8s-anywhere/issues/633), after the runner fix above unblocked pipeline execution | The node's container overlay filesystem was at 88% (49G/59G used), `kubelet` logged `FreeDiskSpaceFailed: Attempted to free 3310057881 bytes, but only found 0 bytes eligible to free` repeatedly, and the node flapped `NodeNotReady`↔`Ready` 17+ times over several hours — consistent with Harbor's `harbor-database-0`/`harbor-jobservice` health-check failures at the time (a 1s healthcheck timeout is too tight for a loaded/IO-contended host). Root cause of the *space*, found 2026-08-06: a stopped-but-not-deleted DR green cluster (`k8s-lab-green`) was holding ~10GB of container volumes despite being fully stopped. | Fixed live 2026-08-06 (`k3d cluster delete k8s-lab-green`, no PR — cluster-lifecycle cleanup, not a git-managed resource): usage dropped 88% → 67% immediately. Final confirmation 2026-08-10: `DiskPressure: False`, 74% (41G/59G), `FreeDiskSpaceFailed` events no longer recurring. | ~5 days (2026-08-05 found → 2026-08-10 confirmed, across several live-cluster sessions — the node also independently flapped `NodeNotReady` from a *separate*, CPU-pressure-driven cause in between, documented on the tracking issue, which delayed closing on the "stable window" criterion specifically, not the disk fix itself). | Tracking issue [#1034](https://github.com/tooming/k8s-anywhere/issues/1034) closed 2026-08-10 (`state_reason: completed`). No mechanical guard added — this was a one-off stale-cluster cleanup, not a recurring pattern; a future item could add a `make status`/`make health` check for orphaned `k3d cluster list` entries if this recurs. |
| 2026-08-06 | P1 | Harbor (all components — `database`, `core`, `jobservice`, `registry`, `portal`, `exporter`) | Live-cluster investigation bringing Harbor up to verify issue #631/#633: components self-inflicted crashloops with no code-level cause. | The `goharbor/harbor` chart hardcodes `timeoutSeconds: 1` as the default for every component's probes. On this resource-constrained host, the underlying exec healthchecks routinely take longer than 1s under any real load, so every component intermittently failed its own healthcheck and got killed/restarted — cascading, since `core` losing its `database` connection mid-restart (or `jobservice` losing `core`) triggers a fresh restart of the dependent component too. | PR #1040 — overrode `timeoutSeconds` to 5s for every probe across all components (15 overrides), matching the convention already used for ArgoCD's own probes elsewhere in this repo. | Same session | Verified live: applied directly to the running Application, `database`/`registry` pods stable with 0 restarts on the new 5s timeout. This is the same bug class the 2026-08-11 `kyverno` row below hit five days later (a different component, same chart-default-1s-probe-timeout footgun) — `scripts/probe-timeout-check.sh` (added later, exact date not tied to this PR) now guards the whole class in `make ci`. |
| 2026-08-07 | P1 | `harbor` namespace ingress NetworkPolicy | Live-cluster investigation working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631): a chronic Envoy 503 survived multiple unrelated fixes (Vault seal, Harbor probe timeouts, a stale EndpointSlice) — traced via Envoy's own `/clusters` admin endpoint (`cx_connect_fail` incrementing against a confirmed-healthy pod IP) plus a same-namespace exec test that worked fine, isolating the failure to cross-namespace NetworkPolicy specifically. | `allow-harbor-ingress.yaml` listed port **80** (Harbor's Service port) instead of **8080** (the `harbor-nginx` pod's actual `containerPort`). NetworkPolicy `ports:` match the destination *pod's* port, not the Service's — this silently blocked every cross-namespace request from Envoy Gateway to Harbor **the entire time this policy has existed**, which is why every previous session's real, distinct fixes on issue #631 (Cilium drift, stale Harbor creds, missing GitLab runner, Vault sealed, probe timeouts, disk pressure) never actually got a signed image through. | PR #1054, merged. | Same session | Verified live: `harbor-networkpolicy` synced to the fix commit, policy showed port 8080; two live pipeline runs afterward failed for a different, already-known reason (host CPU pressure crash-looping Harbor's own pods), confirming this specific bug was the actual blocker, not a remaining cause. No mechanical guard added — a Service-port-vs-pod-port mismatch in a NetworkPolicy is a semantic error `kubeconform`/`kustomize` can't catch (both ports are valid ints); would need a purpose-built cross-reference check (compare each NetworkPolicy's `ports:` against the Service's actual `targetPort`/container `containerPort`) not currently attempted. |
| 2026-08-07 | P2 | Kargo `capstone-pipeline` namespace / Warehouse (issue #633) | Live-cluster session working issue #633: `capstone-pipeline` namespace had been stuck `Terminating` for **20 days**. | A chicken-and-egg deadlock: the namespace's Stage resources' `kargo.akuity.io/finalizer` could never clear because `kargo-webhooks-server` — the pod whose mutating webhook the finalizer-removal path calls — had never actually been running (the whole `kargo` on-demand unit was undeployed), so the namespace could never finish deleting, so ArgoCD could never (re)create the Warehouse in it. Once the controller stack was brought up and the namespace finally cleared, a second, independent bug surfaced: Kargo 1.11.0's admission webhook rejects the capstone `Warehouse` outright — `imageSelectionStrategy: Digest` requires an explicit `constraint` (which tag's digest to watch), and the manifest had none. | Namespace deadlock: fixed live (structural, not git-managed — brought `kargo`'s controller stack up, letting the finalizer clear for the first time in 20 days). Admission rejection: PR #1055 — added `constraint: latest` (the tag this image is actually always pushed as); verified live the Warehouse went from admission-rejected to successfully created for the first time ever. | Same session | New, distinct, pre-existing blocker found (not fixed this session, still relevant): once created, the Warehouse's artifact discovery immediately failed on `harbor.127.0.0.1.nip.io` resolving to `127.0.0.1` — a pod's own loopback, not the host — the same DNS-from-inside-a-pod issue that also leaves capstone's own Deployment/Rollout permanently `ImagePullBackOff`; needs an in-cluster registry mirror (a k3s `registries.yaml` remap), not attempted live. |
| 2026-08-07 | P1 | `envoy-gateway-system` egress NetworkPolicy (`tidb`, `longhorn-system`, `istio-system`, `kargo`) | Clusterless static cross-reference — a remote executor session (janitor fallback) diffed every `kind: HTTPRoute` manifest in `gitops/**` against `allow-envoy-proxy-backend-egress.yaml`'s allowlist, without live-cluster access. | The exact same root cause as the 2026-08-04 `harbor` row above recurred: `allow-envoy-proxy-backend-egress.yaml` hardcodes a closed namespace list, and four more namespaces (`tidb`, `longhorn-system`, `istio-system`, `kargo`) each already had a live HTTPRoute through the shared gateway but were never added to the allowlist — their HTTPRoute responses would be silently dropped by the default-deny floor, unverified live (no cluster access this session) but structurally identical to the confirmed `harbor` failure mode. | PR #1064 (added the four namespaces; also added `scripts/envoy-egress-allowlist-check.sh`, a mechanical recurrence guard — unlike the original `harbor` fix, which added no guard). | Same session | Guard now wired into `make ci` + a `PostToolUse` hook, so a fifth recurrence is caught mechanically instead of needing another live-cluster investigation to notice. A related preventative guard (PR #1065) closes the same footgun *shape* for `networkpolicy-appset.yaml`/`governance-appset.yaml`'s list-generators, currently in sync. |
| 2026-08-10 | P0 | `argocd-repo-server` — cluster-wide (every ArgoCD Application, not just one) | Found `repo-server` crashlooping continuously for **24h+ (54+ restarts)** — every ArgoCD sync/comparison was failing while it cycled, not just work related to issues #631/#633. | Logs showed repeated `Error serving health check request ... duration=5.0-5.4s` right at/over the then-current 5s liveness/readiness `timeoutSeconds` — the probe was cancelling the in-flight healthz request and killing the container before it could finish, an indefinite loop that never converges. This was sustained host latency, not the cold-start CPU-throttling the original 5s value was sized for (per the comment already in `infra/modules/argocd/values.yaml`). | PR #1103 — bumped `repoServer.livenessProbe`/`readinessProbe` and `controller.readinessProbe` `timeoutSeconds` from 5s to 15s, matching the margin already used for Harbor's identical failure mode (PR #1040, row above). Also strengthened `tests/argocd-resources.bats`'s existing `>= 3s` assertion to `>= 10s` so a regression to a "relaxed but still too tight" value is caught mechanically. | Same session | The same chart-default/insufficient-margin probe-timeout bug class as the Harbor (2026-08-06) and Kyverno (2026-08-11, below) rows — three distinct components hit variants of it within 5 days before `scripts/probe-timeout-check.sh` closed the class generically. This row is the most severe instance: unlike Harbor/Kyverno (a single on-demand/next-wave component degraded), a crashlooping `repo-server` is P0 because it blocks ArgoCD's core reconcile loop for the *entire* always-on stack, not one component. |
| 2026-08-11 | P1 | `harbor-registry` (namespace `harbor`) S3 credentials | Live-cluster investigation working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631): `kubectl exec` into the live registry pod confirmed `REGISTRY_STORAGE_S3_ACCESSKEY`/`REGISTRY_STORAGE_S3_SECRETKEY` were never present, and the registry's own logs showed `s3aws: NoCredentialProviders: no valid providers in chain` on every push attempt. | `gitops/platform/harbor.yaml` set `registry.registry.extraEnvVarsSecret: harbor-s3-creds` to inject the registry's S3 credentials — but that field does not exist in the `goharbor/harbor` Helm chart (`helm pull` + `grep -rn extraEnvVarsSecret templates/registry/` against the pinned chart version: zero matches). Helm silently ignores unrecognized keys, so this line had been a complete no-op since the day it was written — the actual, full root cause of every prior push failure on issue #631, not a networking/credential-rotation problem as earlier sessions had assumed. | PR #1114 — switched to the chart's real mechanism (`extraEnvVars` with `valueFrom.secretKeyRef`), verified via `helm template` that it renders real `env:` entries before landing it. | Same session | No mechanical guard added — an unrecognized Helm values key silently no-op'ing is a class this repo has hit more than once (see the `resources:` nesting row below, found two days later in the same investigative arc) but has no generic static check; `helm template` diffing against the chart's actual schema before merging a values change is the manual discipline that would catch it. |
| 2026-08-11 | P1 | `kyverno` admission-controller webhook | Live-cluster investigation working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631), same session as the `harbor-registry` S3-credentials row above: `kubectl describe pod` on both `admissionController` replicas showed their startup probe failing with "connection refused" hundreds of times over 3+ days (one replica logged the failure 788 times over 3d10h alone). | The Kyverno chart's own `startupProbe` default omits `timeoutSeconds` entirely, falling back to Kubernetes' own 1-second default — too tight for this host's actual latency profile, the same chart-default-too-tight footgun already found and fixed the same investigation for Harbor's database/core/registry (PR #1040/#1102) and ArgoCD's repo-server (PR #1103). Because this webhook is fail-closed and `kyverno`'s ArgoCD `Application` has `syncPolicy.automated.selfHeal: true`, a live-only `kubectl patch` to the probes would have been silently reverted by the very next reconcile — an in-git fix was required to hold. | Fixed live and committed to `gitops/platform/kyverno.yaml` the same session (no standalone PR tracked separately from the harbor-registry S3-credentials fix, per that row's PR #1114) — bumped `startupProbe`/`livenessProbe`/`readinessProbe` `timeoutSeconds` from the 1s default to 15s, matching the convention already used elsewhere in this repo for the same host-latency reason. | Same session | Mechanical guard already covers this class: `scripts/probe-timeout-check.sh` (wired into `make ci`, "probe-timeout sanity") asserts every explicit probe across `gitops/`/`infra/` has `timeoutSeconds >= 5s`, so a future PR reintroducing a sub-5s probe timeout (this one included) fails CI instead of silently shipping another chart-default footgun. |
| 2026-08-12 | P2 | GitLab CI (`build-and-push` job, `docker:29-dind` service) | Live-cluster investigation working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631): base-image pulls straight from `docker.io` were failing intermittently but persistently across 4 consecutive pipeline runs (#58–#61), each with an `i/o timeout` on the blob/manifest fetch. | Every failing attempt's destination IP fell in the `100.64.0.0/10` CGNAT range — Colima's VM networking maps some of Docker Hub's CDN edges there, and large/sustained transfers through that path are unreliable on this host (small requests mostly aren't, which is why other, lighter Docker Hub traffic wasn't visibly affected). Confirmed live: direct `docker.io/jaegertracing/example-hotrod` pulls in the `gitlab_default` network timed out across all 4 runs, while `mirror.gcr.io` (Google's public Docker Hub read-through cache) answered the identical request in ~4s over a different edge/path. | Added `--registry-mirror=https://mirror.gcr.io` to the `docker:29-dind` service in `.gitlab-ci.yml` (documented inline at the fix site, no separate PR tracked). Verified live: pipeline #63 built successfully through this exact fix after the 4 prior identical failures. | Same session | No mechanical guard added — this is a live-cluster/host-networking transient (a Colima VM CGNAT routing quirk), not a static-manifest defect this repo's clusterless drift checks could catch; the same class of Colima-VM in-cluster-egress flakiness is cited (not separately guarded either) by the 2026-08-17 `argo-rollouts` row below. |
| 2026-08-13 | P1 | `harbor-registry` (namespace `harbor`) OOMKill | Live-cluster investigation working issue [#631](https://github.com/tooming/k8s-anywhere/issues/631): a real `docker push` of the capstone image OOMKilled the registry container mid-blob-upload (`lastState.terminated.reason: OOMKilled`, `exitCode: 137`, exact timestamp match against the CI job's failed `502 Bad Gateway`). | The `resources:` key for the registry container was nested one level too shallow (`registry.resources` instead of `registry.registry.resources` — a sibling of `registry:`/`controller:`, not inside `registry:`), so it had silently never applied — `kubectl get deploy harbor-registry -o jsonpath` on the live Deployment showed `{}`, confirming this container had never actually had a memory limit applied. Same failure class as the `extraEnvVarsSecret` bug above (a wrong-shaped Helm values key silently ignored). | Commits `38cebf0`/`9fd14c8` (#631) — fixed the nesting and bumped 512Mi→1Gi; verified live via a direct `kubectl patch` + rollout before committing. | Same session | Same gap as the row above: no generic static check for a Helm values key silently not matching the chart's real schema. |
| 2026-08-11 | P0 | `k3d-k8s-lab-server-0` embedded datastore (k3s/kine on SQLite) — control-plane-wide | Live-cluster investigation while debugging an unrelated Harbor/Kyverno issue: `docker logs k3d-k8s-lab-server-0` showed "Slow SQL" warnings on basic kine-table queries (`SELECT MAX(id)`, `compact_rev_key` lookups) taking 1-48 seconds, alongside apiserver TLS handshake timeouts, `FinishRequest ... context deadline exceeded`, and `http: Handler timeout`; `kubectl get nodes` itself failed with a TLS handshake timeout during the investigation. | Confirmed via full `docker logs` scan: k3s's kine background compactor (normally ~5 min cadence, logged as `"COMPACT compacted from X to Y"`) went **completely silent for 16 straight days** after a burst of `"Compact failed: ... transaction has already been committed or rolled back"` errors on 2026-07-25, letting `state.db` grow to 505MB with an 82MB uncheckpointed WAL and turning routine kine queries into full-table scans. A k3s restart earlier the same day had only restored ~6 hours of healthy compaction before the compactor died again — a restart alone is not durable. No k3s flag exists to tune or manually trigger sqlite/kine compaction. | Documented only — this investigation's scope was explicitly clusterless-safe (live datastore surgery on a degraded control plane carries real corruption risk and was intentionally deferred, not attempted). Added `scripts/k3s-datastore-health-check.sh` (+ `make k3s-datastore-health-check`, wired informationally into `make health`) and a "k3s embedded datastore" recovery-cookbook section in `docs/DR.md` (restart → VACUUM → `make down && make up`, cheapest-first). | Unresolved (mitigation options documented; no live remediation performed by design) | Mechanical guard added: `scripts/k3s-datastore-health-check.sh` detects the same signals (compaction gap, state.db/WAL size, Slow SQL volume) via `docker logs`/`docker exec` — deliberately not `kubectl`-based, since the whole failure mode degrades the apiserver itself. Detection only, not prevention: there is no k3s-exposed dial to structurally rule out the compactor stalling again. |

| 2026-08-17 | P0 | `k3d-k8s-lab-server-0` embedded datastore (k3s/kine on SQLite) — control-plane-wide | Interactive session start: `kubectl get nodes` failed with a TLS handshake timeout; `make k3s-datastore-health-check` confirmed the exact signature the 2026-08-11 row documented and flagged as "unresolved." | Confirmed recurrence of the 2026-08-11 incident: the compactor had been silent since **2026-07-25** (23 days by this point — the same stall that row found, never actually resolved, only documented), `state.db` at 482MB with an 88MB WAL, 472 "Slow SQL" warnings in the last 5000 log lines. **Recurred a second time ~25 minutes later, in the same session**, after triggering a real GitLab CI pipeline + an ArgoCD full-tree resync concurrently (issues #631/#633 verification work) — `make k3s-datastore-health-check` failed again (WAL still non-zero, Slow SQL warnings persisting, still zero fresh `COMPACT` lines) — direct evidence for the 2026-08-11 row's "sustained resource-reconciliation churn is the plausible write-pressure driver" theory: this session's own write-heavy actions reproduced the stall live. | Applied `docs/DR.md`'s documented step 1 **twice** this session (`docker restart k3d-k8s-lab-server-0`) — a live-cluster session, unlike 2026-08-11's clusterless one that deliberately deferred remediation. Verified live both times: `kubectl get nodes` succeeded within a couple minutes of each restart, cluster-wide pod health recovered substantially. Even so, the apiserver kept intermittently timing out afterward (`kubectl get pods` failures, one `NotReady` node flap) while CI + ArgoCD reconciliation load continued — restart relieves the datastore symptom but doesn't touch the concurrent CPU-pressure cause. | Same session (~5 min per restart) | Still **not durable** per the 2026-08-11 row's own warning, now confirmed twice in one session — a restart buys time, it doesn't fix the compactor thread dying, and it does nothing for the separate CPU-saturation problem that keeps re-triggering it. No new mechanical guard added (the 2026-08-11 detection script already exists and worked correctly both times); the durable fixes — root-causing the compactor goroutine death, and reducing concurrent write/CPU load during verification work (e.g. don't run a full ArgoCD resync and a CI pipeline at the same time) — remain real gaps. |

| 2026-08-17 | P1 | `argo-rollouts` ArgoCD `Application` / namespace | Live-cluster session working issue [#633](https://github.com/tooming/k8s-anywhere/issues/633) (same session as the second 2026-08-17 P0 datastore row above): `kubectl -n argocd logs argocd-application-controller-0` and the Application's own `.status.conditions` showed the `argo-rollouts` Application had been failing to sync since **2026-08-11** — `ComparisonError: Failed to load target state: failed to generate manifest for source 1 of 1: rpc error: code = DeadlineExceeded desc = context deadline exceeded (retried 5 times)` — with the `argo-rollouts` namespace never actually created, predating this session. | Once the apiserver/datastore issue (the same row's own root cause) was resolved and `argocd-repo-server` was healthy again, a fresh sync queued behind a large post-outage reconciliation backlog (121 Applications) and then failed with a *different*, more specific error: `Error: looks like "https://argoproj.github.io/argo-helm" is not a valid chart repository or cannot be reached: Get "https://argoproj.github.io/argo-helm/index.yaml": context deadline exceeded` — `argocd-repo-server`'s own egress to `argoproj.github.io` timing out from inside the cluster network (reachable fine from the host directly, `curl -m 8` returned 200 in <0.5s) — the same class of Colima-VM in-cluster-egress flakiness this repo has documented elsewhere (`mirror.gcr.io` timeouts in `.gitlab-ci.yml`). Retried twice more; same failure both times. | Not fixed this session — no code changed, only diagnostic information gathered (comment left on issue #633 rather than a PR). Next live-cluster session needs to: (1) retry the sync when repo-server egress to `argoproj.github.io` is reliable — possibly avoid triggering it concurrently with heavy CI/reconciliation load; (2) bring up Kargo; (3) attempt the actual promotion issue #633 asks for. | Unresolved as of 2026-08-17 (not independently re-verified live since — this remote clusterless session cannot confirm current sync health; ADR-0004) | No mechanical guard added — this is a live-cluster transient (repo-server egress reachability), not a static-manifest defect this repo's clusterless drift checks could catch. If it recurs, the fix pattern from the 2026-08-04 `envoy-gateway-system` egress-allowlist row (a mechanical allowlist check) doesn't apply here since the failure is network reachability, not a missing static rule. |

| 2026-08-18 | P2 | Forgejo CI signing pipeline (`build-and-push` + `sign-image` jobs, issue #631) | Live-cluster session continuing issue #631: after 20 days and every previously-logged Harbor/network/credential fix on this issue, still no signed image had landed in Harbor. | Eight distinct, previously-undiscovered bugs found and fixed in sequence, each masking the next: (1) Forgejo + forgejo-runner running under QEMU emulation despite native arm64 images being available — CPU-starved, the chronic crash-on-push/poller-hang root cause; (2) missing `storage` namespace NetworkPolicy ingress rule for harbor→garage traffic, silently dropping every blob upload the whole time, indistinguishable from generic network flakiness; (3) `harbor-registry`'s own QEMU crash class, never triggered before because bug #2 meant it never did real S3 I/O; (4) `registry2`'s S3 driver redirecting blob reads to a cluster-internal-only DNS name, unreachable by any real client; (5) `docker.io`'s auth-token endpoint being the one consistently-unreachable egress hop — mirrored the demo's base image into Harbor itself instead of continuing to retry against it; (6) `sign-image`'s container running as non-root (the `bitnami/cosign` image's default), breaking the `/etc/hosts` write every job's identical step needed; (7) cosign defaulting to fetching a TUF signing config from public Sigstore infra (unreachable, no outbound internet by design); (8) `--allow-insecure-registry` alone not being sufficient to switch cosign from HTTPS to HTTP. | PR #1213 — fixed all eight; independently verified via Harbor's own artifact API (not just the workflow's own success status, ADR-0004) that a real `type: signature.cosign` accessory landed on `harbor.127.0.0.1.nip.io:8080/library/hello:latest`. Added 24 new bats assertions across `tests/forgejo-ci.bats`, `tests/harbor.bats`, `tests/networkpolicy-storage.bats`. | ~20 days across many live-cluster sessions (this session landed the final 8 fixes; the underlying ask had been open on issue #631 since 2026-07-29) | 24 new bats assertions cover the eight specific failure modes found (NetworkPolicy ingress rule presence, non-root cosign workaround, TUF/insecure-registry flags, etc.), so each is now a static-manifest regression a future PR would have to deliberately break, not silently reintroduce. |
| 2026-08-24 | P1 | `kro` (namespace `kro`) | Live-cluster session investigating this host's chronic overload found kro's controller pod in a persistent crash-loop (216+ restarts over the preceding 13 days). | `failed to wait for ResourceGraphDefinition caches to sync` — a downstream symptom of this host's apiserver being too slow to satisfy kro's own informer-sync startup timeout, not an independent bug in kro itself. Each crash-restart triggered a fresh full-cache resync (a burst of List/Watch calls) against the shared k3s datastore, so a chronically-crashing kro was *also* a contributor to the same apiserver/datastore write pressure documented in the 2026-08-11/2026-08-17 P0 rows above, not merely a victim of it. | PR #1300 — suspended kro's `syncPolicy.automated` block and scaled its live replicas to 0 (not a Helm values field; converted to on-demand alongside KEDA in the same PR for general cluster-load reduction). The namespace/RBAC scaffolding (`kro-extras`/`kro-resources`) stays auto-synced so the controller can be brought back without a fresh bootstrap — restore the `automated` block in `gitops/platform/kro.yaml` to re-enable. | Same session (merged 2026-08-25) | Mitigates kro's own contribution to datastore write pressure but does not fix the apiserver-latency root cause; re-enable only once that's durably resolved (per `gitops/platform/kro.yaml`'s own header comment). |
| 2026-08-25 | P1 | `kargo` namespace egress NetworkPolicy (`allow-kargo-egress-registry.yaml`) | Live-cluster session working issue #633: trying to verify a real Kargo promotion, found the Warehouse's image-discovery polling silently timing out against Harbor instead of succeeding or being refused. | Two compounding NetworkPolicy bugs in the same rule: (1) the egress rule's `to:` selected the `harbor` namespace directly, but `gitops/harbor/networkpolicy/allow-harbor-ingress.yaml` only admits ingress to Harbor pods **from `envoy-gateway-system`** — Harbor is reachable only via the shared Gateway, not directly, so the selector never matched the real path; (2) even pointed at `envoy-gateway-system`, the rule listed ports 80/443 (the gateway *Service's* ports) instead of the envoy-proxy *pod's* actual containerPorts (10080/10443) — NetworkPolicy matches the destination pod's port, not the Service's, the same footgun `allow-harbor-ingress.yaml`'s own comment already documents for this exact registry path (see the 2026-08-07 `harbor` ingress NetworkPolicy row above). Together, Kargo's egress to Harbor was silently dropped by the default-deny floor the entire time this rule existed, indistinguishable from generic registry unavailability. | PR #1323 — repointed the selector to `envoy-gateway-system` and the port to 10080 (the route carries no TLS listener, so 443/10443 was correctly left out). Also found and live-patched (not yet gitops-tracked at merge time) a related DNS gap: `harbor.127.0.0.1.nip.io` has no in-cluster DNS answer, worked around with a `coredns-custom` rewrite — brought under GitOps by a follow-up PR #1326 the same day. Verified live via Harbor's own artifact API (not just tool exit codes): `library/hello:latest` pushed and cosign-signed successfully once both fixes landed. | Same session | This is the **fourth** occurrence of the Service-port-vs-pod-port / wrong-namespace-selector NetworkPolicy footgun in this repo (after the 2026-08-04 `harbor` egress-allowlist, 2026-08-07 `harbor` ingress-port, and 2026-08-07 `envoy-gateway-system` four-namespace rows above) — still no generic mechanical guard exists (would need a purpose-built cross-reference checking every NetworkPolicy's `ports:`/`to:`/`from:` against the real Service `targetPort`/pod `containerPort` and the actual upstream ingress policy's allowed source namespace, not attempted). |
| 2026-08-25 | P1 | `envoy-gateway-system` (control plane ↔ data plane xDS stream) | Live-cluster session working issue #633: after PR #1323's NetworkPolicy/DNS fixes cleared every other blocker, Kargo's Warehouse still couldn't discover the pushed/signed image — traced to the envoy-proxy pod's own data plane never actually listening on its route port despite reporting `2/2 Running`. | `envoy-gateway` (the control-plane pod all data-plane proxies pull listener/route config from over a long-lived `DeltaAggregatedResources` gRPC stream) has accumulated 148 restarts over 13 days. Each control-plane restart drops any proxy's already-established xDS stream; per `allow-envoy-proxy-xds-egress.yaml`'s own header comment, this is a known-shaped "LATENT gap" (the stream survives until something restarts, then doesn't self-heal), so a proxy pod can sit indefinitely `2/2 Running` with an empty/stale listener config, serving nothing on its data-plane port — `DeltaAggregatedResources gRPC config stream ... Connection refused`, per PR #1323's own investigation. A clusterless static re-check this session of both NetworkPolicy halves (`allow-envoy-proxy-xds-egress.yaml` egress on TCP 18000 from `component: proxy`; `allow-envoy-controller-xds-ingress.yaml` ingress on the same TCP 18000 to `control-plane: envoy-gateway`) found them correctly matched on port and selector — **not** a recurrence of this repo's three prior NetworkPolicy port/namespace-mismatch incidents (the 2026-08-04 harbor-egress-allowlist, 2026-08-07 harbor-ingress-port, and 2026-08-25 kargo-egress-port/namespace rows above); the xDS policy pair itself looks structurally sound, so the root cause is almost certainly `envoy-gateway`'s own restart frequency, not a manifest bug. | **Resolved same day, PR #1333** (see the row immediately below) — the restart-frequency root cause this row correctly predicted turned out to be `envoy-gateway`'s own probe-timeout footgun, the same class as the Harbor/ArgoCD/Kyverno rows above. Not fixed at the time this row was originally written, hence the account below is left as originally recorded. | Resolved 2026-08-25, same day as found (PR #1333, live-cluster session) | Mechanical guard now covers the underlying cause: `scripts/probe-timeout-check.sh` (see PR #1333's row below). This row's own speculative "likely related to apiserver/datastore write pressure" theory was **not** what PR #1333 found — flagging that the theory was reasonable but wrong, rather than quietly deleting it (ADR-0004: the log records what was actually true, including a corrected earlier guess). |
| 2026-08-25 | P1 | `envoy-gateway` control plane (`envoy-gateway-system` namespace) | Live-cluster session working issue #633, continuing from the row above: found `envoy-gateway`'s Deployment had accumulated 153 restarts over 13 days, all clean `exitCode 0` "Completed" — a liveness-probe-kill signature, not a crash. | The `gateway-helm` chart (pinned `v1.8.3`) hardcodes `livenessProbe`/`readinessProbe` `timeoutSeconds: 1` with no `values.yaml` override hook — the same "chart default too tight for this host" footgun already fixed across Harbor, ArgoCD, and Kyverno (rows above). An earlier, different fix (`leaderElection.disable`, 2026-08-07, PR #1063) had not addressed this — the restart count kept climbing regardless, because it was a separate root cause. During one of these probe-kill restart windows, the data-plane proxy lost its already-established xDS stream to the control plane (per the row above), which is what made the Kargo→Harbor NetworkPolicy fix (PR #1323) insufficient on its own to unblock Warehouse discovery. | PR #1333 — since the chart exposes no values knob for probe timeouts, switched the Application from ArgoCD's native Helm source to a local Kustomize overlay (`gitops/envoy-gateway/`) vendoring the chart via Kustomize's `helmCharts` inflator, then applied a JSON6902 patch bumping both timeouts to 15s. Verified live (`kustomize build --enable-helm` renders the same image/replicas/service-account as the live Deployment, probes confirmed at 15s) and applied live ahead of merge to unblock the #633 investigation. | Same session | `scripts/probe-timeout-check.sh` (already existed from the Kyverno fix) covers this component too — but note the *mechanism* for fixing it differs from every prior instance in this repo: `gateway-helm` is the first pinned chart with genuinely no probe-timeout override hook, requiring the Kustomize-`helmCharts`-vendoring workaround rather than a simple `valuesObject` key. PR #1333 also found and flagged (not fixed) a related dead-config issue: `spec.source.kustomize.buildOptions` is silently ignored by this cluster's live ArgoCD CRD (v3.5.0) across 11 other Application files — a separate, lower-urgency cleanup. |
| 2026-08-11 | P1 | `harbor-database` (namespace `harbor`) SIGPIPE crash-recovery loop | Live-cluster session working issue #631/#633: `harbor-core` showed endless connection failures (DNS-lookup-flavored errors, `unexpected EOF`) that multiple prior sessions had been unable to root-cause. | The earlier Harbor probe-timeout fix (PR #1040, row above) bumped `timeoutSeconds` 1s→5s — not enough under sustained host latency. Killing the exec'd `/docker-healthcheck.sh` mid-script produced `untracked child process ... exited with exit code 141` (SIGPIPE) in postgres's own logs; its supervisor treats that as **fatal**, triggering `terminating any other active server processes` and a full crash-recovery cycle roughly every minute. `harbor-core` was reconnecting to a database that kept restarting out from under it — not a DNS or core bug, as earlier sessions had assumed. | PR #1121 — bumped `harbor-database`'s probe `timeoutSeconds` to 15s (matching the same session's Kyverno/ArgoCD convention) and `failureThreshold` to 6. Also carried a live, previously-uncommitted fix from the same investigation: `infra/modules/argocd/values.yaml`'s `ARGOCD_EXEC_TIMEOUT=300s` — `repo-server`'s `helm pull` subprocess was hitting a separate, tighter 90s internal exec timeout even after the outer gRPC deadline was already bumped (PR #1116). Verified live: database ran stable 7+ minutes (vs. crashing under a minute before); Harbor's front door returned genuine 200s; a CI pipeline got measurably further than any prior attempt. | Same session | `scripts/probe-timeout-check.sh` covers the timeout floor generically, but not the SIGPIPE-specific lesson (a single shared timeout bump can be insufficient if the health-check script itself dies uncleanly under a kill signal, as opposed to simply not finishing in time) — no purpose-built guard for that narrower failure mode exists. |
| 2026-08-11 | P1 | `cert-manager` webhook + `keda` (`operator`/`metricsServer`/`webhooks`) | A dedicated "systemic probe-timeout audit" session, following the Harbor/ArgoCD/Kyverno instances above: swept every remaining always-on chart for the same unaudited default. | Both charts' probes defaulted to the same `timeoutSeconds: 1` footgun (verified against each pinned chart's `values.yaml` — `cert-manager-1.21.1`, `keda-2.20.2`). cert-manager's webhook and KEDA's `webhooks` component are both fail-closed admission webhooks (Certificate/Issuer and ScaledObject/ScaledJob CRDs respectively) — same risk class as Kyverno's, where a probe-timeout crashloop blocks cluster-wide CRD admission, not just the one component. KEDA's `operator`/`metricsServer` aren't fail-closed but sit on the identical footgun. | Bumped all probes (cert-manager's webhook; KEDA's `operator`, `metricsServer`, and `webhooks` — three separate top-level chart keys) to 15s, matching the convention already established for Harbor/ArgoCD/Kyverno. | Same session | `scripts/probe-timeout-check.sh` (already existed) closes the class for these too. This audit — cert-manager, KEDA, plus the earlier Harbor/ArgoCD/Kyverno/harbor-database instances — brings the documented total to **six** always-on/next-wave components hit by the identical chart-default-1s-probe-timeout footgun before the generic guard existed; PR #1333 (Envoy Gateway, row above) is the seventh, found after the guard already existed but needing a different fix mechanism since that chart has no override hook. |

## See also

- [`docs/DR.md`](DR.md#recovery-cookbook-single-component) — per-symptom recovery
  steps; this log adds severity + root-cause context on top of those fixes.
- [`docs/dora-audit-readiness.md`](dora-audit-readiness.md) — Q6/Q8 (Pillar 2) cite
  this file as their evidence.
- [`docs/dora-resilience-mapping.md`](dora-resilience-mapping.md) — Pillar 2 section.
