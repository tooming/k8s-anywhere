{
  "uid": "lab-traces",
  "title": "Lab — Traces",
  "tags": ["lab"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "time": { "from": "now-1h", "to": "now" },
  "templating": {
    "list": [
      {
        "name": "traceql", "label": "TraceQL", "type": "textbox",
        "current": { "text": "{}", "value": "{}" }
      }
    ]
  },
  "panels": [
    {
      "id": 1, "type": "text", "title": "About",
      "gridPos": { "h": 4, "w": 24, "x": 0, "y": 0 },
      "options": { "mode": "markdown", "content": "Traces appear once something **sends spans** to Tempo (OTLP `tempo.observability.svc:4318`). There is no producer yet — Beyla (eBPF) was removed because it can't capture on nested k3d; the planned **OTel-instrumented demo app** will populate this. Edit the **TraceQL** box (e.g. `{ name =~ \".*\" }`, `{ duration > 100ms }`) to filter." }
    },
    {
      "id": 2, "type": "table", "title": "Traces  —  $traceql",
      "gridPos": { "h": 11, "w": 24, "x": 0, "y": 4 },
      "datasource": { "type": "tempo", "uid": "tempo" },
      "targets": [
        { "refId": "A", "datasource": { "type": "tempo", "uid": "tempo" }, "queryType": "traceql", "query": "$traceql", "tableType": "traces", "limit": 50 }
      ],
      "options": { "showHeader": true }
    },
    {
      "id": 3, "type": "traces", "title": "Trace view (click a Trace ID above)",
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 15 },
      "datasource": { "type": "tempo", "uid": "tempo" },
      "targets": [
        { "refId": "A", "datasource": { "type": "tempo", "uid": "tempo" }, "queryType": "traceql", "query": "$traceql", "limit": 1 }
      ]
    }
  ]
}

